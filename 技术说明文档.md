# Wolven Hunt 技术说明文档

## 项目概述

**项目名称**：Wolven Hunt（狼人杀 AI 竞技场）

**项目定位**：一个支持本地部署、多模型对战、实时观赛的 AI 狼人杀引擎与竞技平台

**核心价值**：
- 为 AI 决策研究提供标准化的多智能体博弈测试环境
- 实现完全确定性、可复现的游戏引擎
- 支持真人与 AI 混合对局，探索人机协作模式
- 提供完整的观赛、回放、复盘分析能力

---

## 一、技术架构

### 1.1 整体架构

项目采用**前后端分离 + 事件驱动**架构，核心组件包括：

```
┌─────────────────────────────────────────────────┐
│           Web Frontend (React + TS)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Lobby   │  │  Game    │  │  Replay  │      │
│  └──────────┘  └──────────┘  └──────────┘      │
└──────────────────┬──────────────────────────────┘
                   │ SSE / HTTP API
┌──────────────────┴──────────────────────────────┐
│          Backend (Python + FastAPI)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │   FSM    │  │ Referee  │  │   LLM    │      │
│  │  Engine  │  │ (权限)   │  │ Gateway  │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│  ┌──────────────────────────────────────┐      │
│  │       EventLog (JSONL)               │      │
│  │    单一事实源 (Append-only)          │      │
│  └──────────────────────────────────────┘      │
└─────────────────────────────────────────────────┘
```

### 1.2 技术栈

**后端技术栈**：
- **核心框架**：Python 3.11+、FastAPI、Uvicorn
- **AI 集成**：LiteLLM（统一多模型调用接口）
- **数据校验**：Pydantic v2（类型安全、运行时校验）
- **实时通信**：SSE-Starlette（Server-Sent Events）
- **配置管理**：YAML + Pydantic Settings
- **代码质量**：MyPy（严格类型检查）、Ruff（代码检查）、Pytest + Vitest（72 个测试文件）

**前端技术栈**：
- **核心框架**：React 18、TypeScript 5.6
- **构建工具**：Vite 5（快速开发构建）
- **UI 组件**：Lucide React（图标库）
- **状态管理**：React Hooks + LocalStorage
- **实时通信**：EventSource API（SSE 客户端）

**开发工具链**：
- **包管理**：uv（Python）、npm（JavaScript）
- **版本控制**：Git
- **自动化**：Makefile + Shell Scripts

**代码规模**：
- 后端代码：约 12,400 行 Python（67 个模块）
- 前端代码：约 9,600 行 TypeScript/React
- 测试覆盖：72 个测试文件（后端 44 个 Python + 前端 28 个），包含确定性、属性、泄漏检测等多维度测试

---

## 二、核心技术特性

### 2.1 确定性游戏引擎

**设计目标**：实现完全可复现的游戏过程，支持回放、调试和模型评测。

**技术实现**：
1. **确定性随机数生成器（RNG）**：
   - 基于 `random_seed` 派生所有随机事件
   - 座位分配、平票随机选择均可重现
   
2. **配置驱动**：
   ```python
   GameConfig = RolePack + RuleSet + ModelRoster + PromptPack + random_seed
   ```
   - 每局记录完整配置哈希
   - 支持配置版本管理

3. **事件日志作为单一事实源**：
   - 采用 append-only JSONL 格式
   - 所有游戏状态变更必须对应事件
   - 支持从事件日志完整重建游戏状态

4. **回放与重模拟校验**：
   - 确定性回放：校验事件 `seq` 严格递增、`game_end` 唯一
   - 重模拟（resimulate）：先校验 `raw_response_hash` 与 `config_hash`，再读回 `raw_responses.jsonl` 重跑引擎，逐事件比对 `type / actor / day / phase` 及 canonical payload
   - 任一字段不一致即抛出 `ResimulateDivergence`，可精确定位偏离的事件，防止引擎回归

### 2.2 有限状态机（FSM）设计

**状态转移流程**：
```
GAME_START
  → NIGHT_START
    → NIGHT_GUARD (守卫守护)
    → NIGHT_WOLF_CHAT (狼人夜聊)
    → NIGHT_WOLF_VOTE (狼人投刀)
    → NIGHT_WITCH (女巫行动)
    → NIGHT_SEER (预言家查验)
    → NIGHT_RESOLVE (夜晚结算)
    → CHECK_WIN (胜负检查)
  → DAY_ANNOUNCE (死亡公告)
    → DAY_LAST_WORDS (遗言)
    → DAY_SPEECH (白天发言)
    → DAY_VOTE (投票)
    → DAY_VOTE_PK (PK 重投)
    → DAY_EXILE (放逐)
    → CHECK_WIN
  → 循环至 NIGHT_START
GAME_END
```

**技术亮点**：
- 状态与事件一一对应
- 支持复杂分支逻辑（如 PK、平安日）
- 每个状态都有明确的入口/出口条件

### 2.3 Referee 权限控制系统

**核心职责**：确保信息安全与游戏规则执行。

**三层过滤机制**：

1. **PlayerView 视角过滤**：
   - 每个玩家只能看到符合其角色权限的事件
   - 狼人可见狼队夜聊，其他角色不可见
   - 预言家私有查验结果只对本人可见
   - 守卫、女巫的私有行动不泄漏

2. **动作合法性校验**：
   ```python
   # 示例：守卫不能连续守护同一目标
   def validate_guard_action(target, last_target):
       if target == last_target:
           return ValidationError("不能连续守护同一目标")
   ```
   - 预查验阶段拒绝非法动作
   - 触发重试与 fallback 机制

3. **文本事实一致性检查**：
   - 检测玩家发言是否违反已知信息边界
   - 拦截守卫声称"挡刀成功"等越权陈述
   - 防止未轮到玩家被描述为"沉默"或"划水"
   - 违规文本触发 `illegal_action` 处理

**Spectator 模式**：
- 观众视角：可见所有座位身份、狼人夜聊、特效
- 隐私保护：不暴露 raw response、API key、模型配置

### 2.4 多模型 LLM 集成

**技术方案**：
- 使用 **LiteLLM** 作为统一网关
- 支持 OpenAI、Claude、Gemini、本地模型等
- 每个座位可独立配置模型

**Per-Seat 路由**：

`provider` 字段只区分 `mock`（本地模拟，不联网）与 `litellm`（经 LiteLLM 调用真实模型）。具体厂商由 `model` 与 `base_url` 决定，不同座位可指向不同的兼容 OpenAI 接口的服务。

```yaml
# 示例：座位 1 走 GPT-4，座位 2 走 Claude，均通过 litellm
seat_1:
  provider: litellm
  model: gpt-4-turbo
  base_url: https://api.openai.com/v1
  api_key: sk-xxx

seat_2:
  provider: litellm
  model: claude-3-opus
  base_url: https://api.anthropic.com
  api_key: sk-ant-xxx
```

**密钥管理**：
- API 密钥不提交到仓库
- 为避免API KEY被恶意盗用，请联系开发者获取对应agent的密钥

**成本跟踪**：
- 记录每次 LLM 调用的 token 消耗
- 支持成本分析与优化

### 2.5 真人与 AI 混合对局

**核心特性**：
- 一局最多 1 个真人座位，其余 9 个座位由 AI 托管
- 真人只能访问自己座位的 PlayerView
- 通过 `player_token` 进行座位鉴权

**技术实现**：

1. **真人建局流程**：
   - 选择座位（指定或随机）
   - 选择角色（村民/狼人/神职/随机）
   - 发牌时进行确定性角色交换，不改变角色计数

2. **真人动作提交**：
   ```http
   POST /games/{game_id}/seat/{seat}/action
   Authorization: Bearer {player_token}

   {
     "kind": "vote",
     "target": 3
   }
   ```
   `kind` 取值对应当前回合的动作类型，如 `guard`、`wolf_chat`、`wolf_vote`、`seer`、`witch`、`speech`、`vote`、`pk_vote`、`last_words`。

3. **真人 SSE 订阅**：
   - 独立的座位级事件流
   - 严格的 Referee 过滤
   - 实时回合提示
   - `turn_request` / `turn_cleared` 只是 SSE 投影，不进入 EventLog，不影响 replay hash

4. **前端辅助功能**：
   - 身份标注（仅存储在浏览器 LocalStorage）
   - 预言家查验结果锁定（只标记阵营，不暴露具体角色）
   - 不影响后端 EventLog、Prompt Evolution 训练窗口和 replay

### 2.6 实时观赛系统

**技术方案**：基于 SSE（Server-Sent Events）的推送架构

**观赛特性**：
1. **实时事件推送**：
   - 发言、投票、夜晚行动
   - 音效触发、特效展示
   - 倒计时、投票直方图

2. **Pacing 控制**：
   - 支持 `live`、`fast`、`off` 三种节奏
   - 前端 ack 机制解除等待
   - ack 只控制现场观赛节奏，不进入 EventLog，不影响 replay hash

3. **观赛特效投影**：

   观赛特效不是独立的 EventLog 事件，而是由后端从 EventLog 实时派生的 API/SSE 投影（`GET /games/{id}/effects`）。每个 `SpectatorEffect` 的结构如下：

   ```json
   {
     "seq": 42,
     "day": 1,
     "phase": "NIGHT_GUARD",
     "kind": "guard_shield",
     "actor": 8,
     "source_seat": 8,
     "target_seat": 3,
     "asset_key": "guard_shield",
     "duration_ms": 2000,
     "meta": {}
   }
   ```

   `kind` 取值包括 `guard_shield`、`wolf_attack`、`seer_vision`、`witch_potion`、`death_reveal`，用于驱动前端的夜晚特效与音效。该投影只面向 Referee 过滤后的 spectator 视角，不进入普通 PlayerView、prompt、narrative 或 replay hash，也不得暴露 raw response、provider 配置、prompt、模型名或玩家不可见事件原文。

4. **叙事化事件流**：
   - `narrative.jsonl`：观众友好的中文叙事
   - 自动生成游戏摘要
   - 支持关键时刻高亮

### 2.7 赛后复盘系统

**复盘报告生成**：
```http
POST /games/{game_id}/review-report
```

**报告内容**：
- 游戏进程摘要
- 关键决策分析
- 阵营胜负原因
- 玩家表现评估

**隐私保护**：
- 只使用 spectator-safe events、`narrative.jsonl`、`final_reveal.json` 与 `seat_presentation` 派生输入
- 不读取或展示 `raw_responses.jsonl`
- 不暴露 API key、provider 配置、模型名、prompt 原文或玩家私有视角
- 生成报告不改变 EventLog、SSE、历史列表或 replay hash

**持久化存储**：
```
runs/{game_id}/
├── manifest.json          # 配置、seed、时间戳
├── events.jsonl           # 完整事件日志
├── narrative.jsonl        # 叙事流
├── final_reveal.json      # 终局揭示
├── review_report.json     # 复盘报告
└── raw_responses.jsonl    # LLM 原始响应（私有）
```

---

## 三、技术创新点

### 3.1 确定性多智能体系统

**行业痛点**：传统 AI 对局系统难以复现和调试。

**创新方案**：
- 完全确定性的游戏引擎
- 事件日志作为单一事实源
- 确定性回放 + 重模拟逐事件校验机制
- 支持从 raw_responses 重跑引擎验证一致性

**应用价值**：
- 模型评测：公平比较不同模型表现
- 调试优化：精确定位决策问题
- 学术研究：可复现的实验环境

### 3.2 分层权限控制架构

**技术挑战**：狼人杀是不完全信息博弈，信息泄漏会破坏游戏公平性。

**创新设计**：
- Referee 三层过滤（视角/动作/文本）
- 运行时文本事实一致性检查
- 观众/玩家/调试三种视角模式

**技术难点突破**：
- 实时检测 LLM 输出的越权陈述
- 动态生成角色特定的 prompt context
- 保证信息隔离的同时支持并发调用

### 3.3 人机混合对局系统

**设计理念**：真人与 AI 无缝协作，而非简单并存。

**技术保障**：
- 真人动作与 AI 决策统一进入 FSM
- 真人视角严格遵守 Referee 过滤
- 支持真人局的确定性回放

**扩展性**：
- 架构支持扩展至多真人模式
- 可用于人类玩家训练 AI
- 支持人类专家数据采集

### 3.4 可配置的 Prompt Evolution

**背景**：狼人杀策略复杂，固定 prompt 难以覆盖所有场景。

**技术方案**：
- 基于复盘报告自动生成新 prompt 版本
- Prompt 包版本化管理（不可变快照）
- 支持 A/B 测试不同 prompt 效果

**工程实践**：
- 不改变事件 schema 和胜负判定
- 不影响 Referee 权限边界
- 默认关闭，通过配置开关启用

---

## 四、工程质量保障

### 4.1 代码质量

**类型安全**：
- Python 使用 MyPy strict 模式
- TypeScript 严格模式
- Pydantic 运行时校验

**代码规范**：
- Ruff 代码检查（E, F, I, B, UP, SIM, RUF）
- 统一代码格式（100 字符行宽）
- 完整的类型注解

### 4.2 测试体系

**测试类型**：
1. **确定性测试（Golden Tests）**：
   - 验证相同 seed 产生相同结果
   - 检查事件序列完整性

2. **属性测试（Property Tests）**：
   - 使用 Hypothesis 进行模糊测试
   - 验证不变量（如角色总数、胜负条件）

3. **泄漏检测测试（Leakage Tests）**：
   - 验证 PlayerView 不泄漏私有信息
   - 检查 Referee 过滤正确性

4. **LLM 集成测试**：
   - Mock provider 单元测试
   - 真实 LLM smoke test（需环境变量开启）

5. **前端测试（Vitest）**：
   - 28 个测试文件，覆盖游戏状态、SSE 流、音效节奏、投票直方图、真人回合等
   - 通过 `npm run test:frontend` 运行

**测试覆盖**：
- 72 个测试文件（后端 44 个 Python + 前端 28 个 TypeScript）
- 覆盖核心引擎、Referee、LLM 网关、前端交互等

### 4.3 可观测性

**运行时监控**：
- 完整的事件日志记录
- LLM 调用跟踪（token、耗时、成本）
- 错误与 fallback 记录

**调试支持**：
- 健康检查端点：`/healthz`
- API 文档：`/docs`（FastAPI 自动生成）
- 详细的错误日志

### 4.4 部署友好

**一键启动**：
```bash
./start.sh  # macOS/Linux
start.bat   # Windows
```

**环境检查**：
- 自动检测依赖（Python、Node.js、uv）
- 友好的错误提示
- 支持本地 AI 助手辅助排查

**跨平台支持**：
- macOS / Linux / Windows
- Python 3.11 - 3.13 测试通过

---

## 五、技术难点与解决方案

### 5.1 并发调用与确定性保证

**挑战**：
- 同时投票时需要并发调用 LLM
- 但事件必须按确定性顺序记录

**解决方案**：
```python
# 并发收集决策
decisions = await asyncio.gather(*[
    agent.decide(seat) for seat in alive_seats
])

# 确定性排序后写入 EventLog
for seat in sorted(alive_seats):
    event_log.append(vote_event(seat, decisions[seat]))
```

**关键点**：
- 并发调用基于同一份 GameState 快照
- 决策在内存中暂存，不立即写入日志
- 结算时按座位号升序追加事件

### 5.2 文本事实一致性检查

**挑战**：
- LLM 可能生成违反游戏规则的发言
- 需要实时检测并拒绝

**技术方案**：

1. **规则库**：
   ```python
   # 示例规则
   - 守卫不能声称连续守护同一人（违反游戏机制）
   - 不能描述未轮到的玩家为"沉默"（违反事实）
   - 不能引用未授权的狼聊内容（越权）
   ```

2. **检测流程**：
   ```
   LLM 输出 → Referee 文本检查 → 发现违规 → 触发重试
   → 仍违规 → Fallback 机制 → 使用安全模板
   ```

3. **Fallback 机制**：
   - 默认 `max_retries: 2`，即最多 2 次重试、3 次尝试
   - 仍失败时使用预设的安全发言模板
   - 记录 fallback 事件供调试

### 5.3 夜晚行动顺序与结算

**复杂性**：
- 女巫需要知道狼刀目标
- 守卫和女巫可能同时保护同一目标（双奶死）
- 毒药和狼刀可能命中同一目标

**结算逻辑**：
```python
def night_resolve(guard_target, wolf_target, witch_action):
    # 1. 双奶判定
    if guard_target == wolf_target and witch_action.save == wolf_target:
        return Death(wolf_target, reason="double_save")
    
    # 2. 单独解药
    if witch_action.save == wolf_target and guard_target != wolf_target:
        wolf_target_survives = True
    
    # 3. 单独守护
    if guard_target == wolf_target and witch_action.save != wolf_target:
        return NoDeathTonight()
    
    # 4. 毒药独立结算
    if witch_action.poison:
        return Death(witch_action.poison, reason="poison")
```

**技术保障**：
- 明确的优先级规则
- 完整的测试覆盖
- 事件日志记录所有中间状态

### 5.4 真人局的 EventLog 兼容性

**挑战**：
- 真人动作需要用户输入，打破确定性
- 但回放系统要求完全确定性

**解决方案**：

1. **EventLog 存储真人动作**：
   ```jsonl
   {"event": "vote_cast", "actor": 5, "target": 3, "agent_kind": "human"}
   ```
   - 真人动作照常进入 EventLog
   - 回放时直接读取，无需重新执行

2. **UI 交互事件不入库**：
   ```jsonl
   # 不进入 EventLog
   - turn_request (前端提示轮到真人)
   - turn_cleared (前端清除提示)
   - ack (观赛节奏确认)
   ```
   - 这些是 SSE 投影，不影响游戏逻辑

3. **角色交换机制**：
   - 发牌先确定性 shuffle
   - 再进行角色 swap（真人指定角色时）
   - swap 参数随 manifest 持久化

---

## 六、性能与扩展性

### 6.1 性能指标

**响应时间**：
- API 响应：< 100ms（不含 LLM 调用）
- SSE 事件延迟：< 50ms
- 前端渲染：60fps 流畅动画

**并发能力**：
- 单机支持多局并行
- 每局独立 EventLog，无竞态条件

**存储效率**：
- 单局 EventLog：通常 < 1MB
- JSONL 格式，支持增量读取
- 历史对局自动归档

### 6.2 可扩展性设计

**配置驱动**：
- 新增角色/规则无需改代码
- YAML 配置文件管理
- 支持自定义板子

**模块化架构**：
```
core/       # 核心引擎（角色无关）
agents/     # AI 决策层
referee/    # 权限控制
llm/        # LLM 集成
api/        # HTTP/SSE 接口
storage/    # 持久化
```

**扩展方向**：
- 支持更多角色（猎人、白痴、骑士）
- 多真人模式
- 竞技排名系统
- 实时对战匹配

---

## 七、应用场景

### 7.1 AI 研究

**模型评测**：
- 公平比较不同模型的狼人杀能力
- 确定性环境，可复现结果
- 自动生成评测报告

**策略研究**：
- 研究最优发言策略
- 分析身份伪装技巧
- 探索多智能体协作

### 7.2 娱乐体验

**观赛模式**：
- 观看 AI 对局，学习游戏策略
- 实时音效、特效、倒计时
- 支持历史回放

**真人参与**：
- 与 9 个 AI 同场竞技
- 测试人类与 AI 的策略差异
- 趣味性与挑战性兼具

### 7.3 教育培训

**游戏教学**：
- 新手观看 AI 对局学习规则
- 分析优秀玩家的决策逻辑

**程序设计教学**：
- FSM 设计实例
- 事件驱动架构示范
- 多智能体系统实践

---

## 八、未来规划

### 8.1 短期优化

**性能优化**：
- LLM 调用缓存
- 事件日志压缩
- 前端资源懒加载

**用户体验**：
- 更丰富的观赛特效
- 移动端适配
- 多语言支持

### 8.2 功能扩展

**游戏内容**：
- 新增角色（猎人、骑士、白痴）
- 支持 12 人局、屠城局
- 自定义板子编辑器

**社交功能**：
- 多真人模式
- 房间系统
- 对战记录与排行榜

### 8.3 技术演进

**分布式部署**：
- 支持多节点负载均衡
- Redis 缓存层
- 数据库持久化

**AI 能力增强**：
- 集成语音 TTS/STT
- 多模态模型支持
- 强化学习训练

---

## 九、总结

Wolven Hunt 是一个**技术完整、架构清晰、工程严谨**的 AI 狼人杀项目。

**技术亮点**：
1. ✅ **确定性引擎**：完全可复现的游戏过程
2. ✅ **权限控制**：三层过滤保障信息安全
3. ✅ **多模型集成**：灵活的 LLM 接入能力
4. ✅ **人机混合**：真人与 AI 无缝协作
5. ✅ **实时观赛**：流畅的 SSE 推送体验
6. ✅ **复盘分析**：完整的赛后分析能力

**工程质量**：
- 约 22,000 行代码（后端 12,400+ 前端 9,600）
- 72 个测试文件（后端 44 + 前端 28）
- 严格类型检查
- 完整文档覆盖

**创新价值**：
- 为 AI 决策研究提供标准化测试环境
- 探索人机协作的新模式
- 推动多智能体系统工程化实践

项目已实现**本地一键启动**，支持 macOS/Linux/Windows，配置灵活，易于部署和二次开发。

---

## 附录

### A. 技术栈版本

| 组件 | 版本 |
|------|------|
| Python | 3.11+ |
| Node.js | 20+ |
| FastAPI | 0.115+ |
| React | 18.3 |
| TypeScript | 5.6 |
| Vite | 5.4 |
| LiteLLM | 1.50+ |
| Pydantic | 2.6+ |

### B. 核心 API 端点

| 端点 | 方法 | 功能 |
|------|------|------|
| `/games` | GET | 历史对局列表（不含 API key 或 raw response） |
| `/games` | POST | 创建游戏 |
| `/games/{id}/run` | POST | 开始游戏 |
| `/games/{id}/events` | GET | Referee 过滤后的 spectator 事件 |
| `/games/{id}/stream` | GET | 观众 SSE 流 |
| `/games/{id}/narrative` | GET | 叙事流 |
| `/games/{id}/effects` | GET | 观赛特效投影 |
| `/games/{id}/reveal` | GET | 终局揭示 |
| `/games/{id}/review-report` | GET | 获取已生成复盘报告 |
| `/games/{id}/review-report` | POST | 生成复盘报告 |
| `/games/{id}/ack` | POST | 解除现场观赛 pacing 等待，不写 EventLog |
| `/games/{id}/seat/{seat}/stream` | GET | 真人 SSE 流 |
| `/games/{id}/seat/{seat}/action` | POST | 真人动作提交 |
| `/models/test` | POST | 后端代理模型连通性测试，不落盘、不回显 API key |

### C. 项目资源

- **项目仓库**：`/Users/tampouseng/Desktop/Wolven Hunt`
- **文档**：
  - README.md：快速开始指南
  - architecture.md：架构契约
  - QUICKSTART.md：快速参考
  - AGENTS.md：开发约束
- **配置目录**：`configs/`
- **历史对局**：`runs/`

### D. 启动命令

```bash
# 一键启动
./start.sh                 # macOS/Linux
start.bat                  # Windows

# 开发模式
make dev                   # 前后端热重载

# 生产模式
make serve-prod            # 构建前端 + 启动服务

# 测试
make test                  # 运行全部测试
make test-fast             # 快速测试
make typecheck             # 类型检查
make lint                  # 代码检查

# 模拟对局
make simulate              # 无 UI 模拟
```

### E. 联系方式

如有技术问题或合作意向，请通过项目文档中的渠道联系。

---

**文档版本**：1.0  
**更新日期**：2026-06-05  
**适用对象**：技术评审、项目评估、学术交流
